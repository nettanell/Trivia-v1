# CLAUDE.md — כללי עבודה על אפליקציית הטריוויה

PWA סטטית בעברית (RTL), בלי build ובלי תלויות. כל שינוי נכנס ישירות לקבצים.

**לפני כל כתיבה או שינוי של שאלות — לקרוא את `LEARNER.md` במלואו** (מי הלומד ומה נחשב שאלה טובה).

## מבנה הקבצים

| קובץ | תוכן |
|---|---|
| `index.html` | כל ה-UI, ה-CSS והלוגיקה. כל המסכים יושבים בתוך `div.app`. |
| `questions.js` | בנק התוכן: `Q` (שאלות פתוחות: `c,q,h,a,m,d`; `k` = נוסח ישן של השאלה, מפתח ההתקדמות), `FIX_A`/`FIX_B`/`FIX_R` (שכתובי הסברים שמוחלים על `Q` בסוף הקובץ), `VQ` (אמריקאיות, `o[0]` = התשובה הנכונה), `DQ` (האתגר היומי, `h:1` = קשה מאוד, `o[0]` נכונה), `PATHS` (מסלולי לימוד), `REVQ` + `REV_LINK` (שאלות הפוכות שמקושרות לשאלת המקור ויורשות ממנה הסבר/העמקה), `DEEP3` (העמקות `d` לפי טקסט השאלה). |
| `sw.js` | Service worker. הקבוע `CACHE` (עכשיו `trivia-v25`) ורשימת `SHELL`. |
| `photos.js` | רשימת התמונות לתיקיית `pics`. |
| `manifest.webmanifest` | מניפסט ה-PWA. |

## כללי תוכן — כל שאלה חדשה

- **הסבר לחסר רקע** (הכלל המלא ב-`LEARNER.md`): משפט ראשון = כל הרעיון; אף מונח לא נחשב ידוע; מנגנון בשלבים ממוספרים עם `\n`. `a`/`m`/`d` שומרים ירידות שורה.
- **הסבר אמיתי חובה**: לכל שאלה צריך הסבר שמלמד משהו (`a` מלא ו/או `m`). לא רק "התשובה היא X".
- **העמקה (`d`) כשאפשר**: למה זה חשוב, איך זה עובד, דוגמה מהחיים. דרך `d` בשאלה עצמה או ב-`DEEP3`.
- שאלה הפוכה (`REVQ`) חייבת רשומה ב-`REV_LINK` שמפנה לשאלת המקור.

## בדיקות חובה לפני שילוח שאלות

1. **דדופ מול כל הבנק**: hash לפי טקסט השאלה (`q`, מנורמל: trim, רווחים, פיסוק) מול כל המאגרים (`Q`, `VQ`, `DQ`, `REVQ`, ...). שאלה שה-hash שלה כבר קיים לא נכנסת.
2. **אנלייזר ניחושיות** (בשאלות עם אפשרויות): התשובה הנכונה לא יכולה להיות
   - הארוכה ביותר מבין האפשרויות;
   - היחידה עם סוגריים;
   - היחידה עם מקף.
   אם כן — לשכתב את האפשרויות (להאריך/להוסיף סוגריים למסיחים, או לקצר את הנכונה).
3. **בדיקת איכות ויזואלית**: לפתוח את האפליקציה ב-headless (Playwright/Chromium), לצלם 3–4 כרטיסים אקראיים מהשאלות החדשות ולעבור על התוכן: ניסוח, הסבר, העמקה, תצוגה ב-RTL.

## דיפלוי

- **כל דיפלוי מעלה את שם ה-cache ב-`sw.js`** (`trivia-v25` → `trivia-v26` → ...). בלי זה משתמשים יישארו עם גרסה ישנה.
- קובץ חדש שהאפליקציה צריכה אופליין — להוסיף ל-`SHELL`.

## כללי UI / UX

- **ניווט בין מצבים רק בסרגל התחתון** (`.nav`). לא להוסיף כפתורי מעבר בין מצבים במקומות אחרים.
- **ראש-בראש**: אין בדיחות ואין עקיצות בין סבבים.
- **אין שלבים בין חשיפת התשובה לדירוג**: מיד אחרי שהתשובה נחשפת מופיע הדירוג, בלי מסך/לחיצה באמצע.
- **מסכים חדשים בתוך `div.app`**, לא מחוץ לו.
